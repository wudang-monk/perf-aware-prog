bits 16

sub bx, 5
;; add cx, -90
;; add cx, 90
;; mov ax, cs
;; mov ss, ax
;; mov ds, bx
;; mov es, cx

;; mov sp, ss
;; mov bp, ds
;; mov si, es
;; mov di, dx

;; mov bx, 0x2222
;; mov bl, 0x33
;; mov bh, 0x55
;; mov es, bx
;; mov bx, ss
;; mov ss, al + 72
;; mov ss, 72
;; mov [ss + 12], 72
;; mov [ss + 872], 72
;; mov [ss + 8728], 72

;; mov ax, ss
;; mov [ax + 72], ss
;; mov [al + 72], ss
